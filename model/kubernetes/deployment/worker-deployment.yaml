apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-worker
spec:
  selector:
    matchLabels:
      app: redis-worker
  replicas: 1
  template:
    metadata:
      labels:
        app: redis-worker
    spec:
      containers:
        - name: worker
          image: redis-demo-worker
          imagePullPolicy: Never
          volumeMounts:
            - mountPath: /usr/src/app/config
              name: config-data-volume
      volumes:
        - name: config-data-volume
          configMap:
            name: config
