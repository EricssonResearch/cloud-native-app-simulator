apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-worker2
spec:
  selector:
    matchLabels:
      app: redis-worker2
  replicas: 1
  template:
    metadata:
      labels:
        app: redis-worker2
    spec:
      containers:
        - name: worker
          image: redis-demo-worker
          imagePullPolicy: Never
          volumeMounts:
            - mountPath: /usr/src/app/config
              name: config-data-volume
      volumes:
        - name: config-data-volume
          configMap:
            name: config2
