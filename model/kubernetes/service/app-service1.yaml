apiVersion: v1
kind: Service
metadata:
  name: redis-service1
spec:
  selector:
    app: redis-demo1
  ports:
    - name: http
      port: 5000
      targetPort: 5000
    - name: tcp
      port: 6379
      targetPort: 6379
# ---
# apiVersion: v1
# kind: ServiceAccount
# metadata:
#   name: example-demo1
#   labels:
#     account: demo1
